This folder is synced to dropbox and can be cloned from GitHub with

rm -rf ~/shell && git clone https://github.com/gatogatogato/shell ~/shell
zsh ~/shell/deploy.sh

That way you can quickly set up your ZSH.